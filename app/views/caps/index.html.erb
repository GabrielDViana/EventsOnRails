<!-- <p id="notice"><%= notice %></p> -->
<%= render 'layouts/inner_header' %>
<h1>Caps</h1>

<% tag_cloud ActsAsTaggableOn::Tag.all, %w[btn] do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
<% end %>

<table class="table">
  <thead>
    <tr>
      <th>Dono</th>
      <th>Titulo</th>
      <th>Area</th>
      <th>Data e Horario</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @caps.each do |cap| %>
      <tr>
        <td><%= cap.user.nickname %></td>
        <td><%= cap.title %></td>
        <td><%= cap.area %></td>
        <td><%= cap.date.strftime(" %d/%m/%Y %H:%M") %></td>
        <td><%= link_to 'Show', cap %></td>
        <% if logged_in? && current_user = cap.user%>
            <td><%= link_to 'Remove', cap_path(cap), method: :delete,
                data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        <td><%= link_to 'Participar', new_cap_participation_path(cap) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if logged_in? %>
<%= link_to 'New', new_cap_path %>
<% end %>
